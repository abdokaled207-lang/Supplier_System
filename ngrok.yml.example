version: "2"
# Replace this placeholder with your real ngrok authtoken from the ngrok dashboard.
# Never commit a real token to this repository — ngrok.yml is gitignored.
authtoken: YOUR_NGROK_AUTHTOKEN_HERE
tunnels:
  frontend:
    addr: 5173
    proto: http